apiVersion: v1
kind: Service
metadata:
  name: notesbhej
  namespace: notesbhej
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: notesbhej
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
      nodePort: 30000
