apiVersion: v1
kind: Service
metadata:
  name: notesbhej-stage
  namespace: notesbhej-stage
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: notesbhej-stage
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
      nodePort: 31000
