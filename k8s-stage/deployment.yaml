apiVersion: apps/v1
kind: Deployment
metadata:
  name: notesbhej-stage
  namespace: notesbhej-stage
  labels:
    app.kubernetes.io/name: notesbhej-stage
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: notesbhej-stage
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notesbhej-stage
    spec:
      containers:
        - name: app
          image: docker.io/manojthedonut/notesbhej:stage
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: notesbhej-stage-config
            - secretRef:
                name: notesbhej-stage-secrets
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      securityContext:
        seccompProfile:
          type: RuntimeDefault
